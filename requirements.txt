# TTAS RAG + LLM 檢傷分級系統相依套件
# 安裝順序請參考下方說明

# 基礎資料套件
pandas==2.2.3
xlrd==2.0.2
pdfplumber==0.11.9

# 視覺化
matplotlib==3.10.8
seaborn==0.13.2

# 向量資料庫
chromadb==1.0.21

# 嵌入模型（已安裝 sentence-transformers==5.2.2，無需重裝）
# sentence-transformers>=2.2.0

# torch
# pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124 

# GPU 版 llama-cpp-python（CUDA 12.4 預編譯 wheel，需另行安裝）
# $env:CMAKE_ARGS = "-DGGML_CUDA=on"
# pip install llama-cpp-python --no-cache-dir --force-reinstall

# API 服務
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
